# 1mwapp — memory root

Baseline auto-seed for portable Serena onboarding. Expand with non-obvious invariants as you learn them.

## Overview
- Repo: `1mwapp`
- Stack signals: typescript/next
- Serena LSs: typescript
- Notes from docs: 1MW WebApp

## Graph
- Languages / tools: `mem:tech_stack`
- Commands: `mem:suggested_commands`
- Style: `mem:conventions`
- Done gates: `mem:task_completion`

## Invariants to preserve
- Prefer symbol tools over raw file dumps for code navigation.
- Don't commit `.serena/cache`, logs, or `project.local.yml` (see `.serena/.gitignore`).
- Update memories when durable project conventions change; skip one-off task notes.
