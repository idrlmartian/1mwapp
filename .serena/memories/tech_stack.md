# Tech stack — 1mwapp

- Detected languages/frameworks: typescript/next
- Package manager / build: yarn
- Serena language_servers: typescript
- Package scripts (if any): build, dev, pm, prod, start

Refine this file when you learn version pins that matter (e.g. Next major, Rust edition).
